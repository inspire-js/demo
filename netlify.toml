[build]
  # `npm install` triggers the nudeps `prepare` hook, which copies deps into
  # client_modules/ and generates importmap.js. `publish` is the repo root.
  command = "npm install"
  publish = "."

[[headers]]
  for = "/*"
    [headers.values]
    Access-Control-Allow-Origin = "*"
